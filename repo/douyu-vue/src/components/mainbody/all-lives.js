import PlayBoxes from './all-lives/play-boxes';
import PlayBox from './all-lives/play-box';

export default {
    render(h) {
        return <div>
                    <PlayBoxes>
                        <PlayBox/>
                    </PlayBoxes>
               </div>
    }
}
